This translation is used for a VPN app UI (mobile/desktop): keep strings short; button and menu labels stay concise and imperative.

- The brand name "VpnHood" — alone, as "VpnHood!", or in product names like "VpnHood! CLIENT", "VpnHood! CONNECT" — never changes: never translate or transliterate it; always keep it in Latin script.
- Company and platform names (GitHub, Google Play, App Store, Windows, Android, iOS) never change.
- Preserve placeholders like {serverName} or %s exactly as written.

Return the resource value if the key exactly matches any of the following:
- "SYSTEM_DEFAULT_LANGUAGE_NOT_SUPPORTED_DESC"
- "VPN_HOOD_APP_NAME"
- "VPN_HOOD_CONNECT_APP_NAME"
- "SEND_FEEDBACK_URL"
- "PROTOCOL_UDP"
- "PROTOCOL_TCP"
- "KILL_SWITCH"
- "KILL_SWITCH_COLORED"
- "VPNHOOD_ENGINE"
- "DNS"

Return the resource word if it exactly matches any of the following:
- "Kill Switch"
